<h1>Restaurant List</h1>
<ul>
  <% @restaurants.each do |resto| %>
    <li><%= link_to resto.name, restaurant_path(resto) %></li>
  <% end %>
</ul>

<%= link_to "Add a restaurant", new_restaurant_path, class: "btn-green"%>
